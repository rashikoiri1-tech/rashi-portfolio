{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack Portfolio for Rashi Koiri with React, TypeScript, Express, and Canvas Animation",
  "scripts": {
    "dev": "node dev.js",
    "install:all": "npm install && npm run install:backend && npm run install:frontend",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "npm install --prefix backend",
    "generate:frames": "node scripts/generate-frames.js",
    "generate:images": "node scripts/generate-project-images.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm run start --prefix backend"
  },
  "keywords": [
    "portfolio",
    "full-stack",
    "react",
    "vite",
    "typescript",
    "express",
    "mysql",
    "rashi-koiri"
  ],
  "author": "Rashi Koiri",
  "license": "MIT"
}
